spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/sais?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=GMT%2B8
    tomcat:
      initSQL: SET NAMES utf8mb4
  session:
    store-type: redis   #session存储类型
    timeout: 2000  #设置session超时时间
  redis:
    host: localhost
    port: 6379
